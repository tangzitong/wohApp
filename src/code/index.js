import Vue from 'vue'
import StoreCache from '@/utils/storeCache'

Vue.use(VueCode)
const cache = new StoreCache('vuex')

export function getAreaConfig() {
 }

export function setAreaConfig(lang) {
}

export function getIndustryConfig() {
}

export function setIndustryConfig(lang) {
}

const messages = {
}

const code = new VueCode({
})

export default code
